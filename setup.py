from setuptools import setup

setup(name='redisdb',
      version='0.1',
      description='redisdb',
      url='',
      author='Knightingale',
      author_email='',
      license='',
      packages=['redisdb'],
      install_requires=['redis'
      ],
      zip_safe=False)
